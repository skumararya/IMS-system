import { BlackonhoverDirective } from './blackonhover.directive';

describe('BlackonhoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BlackonhoverDirective();
    expect(directive).toBeTruthy();
  });
});
